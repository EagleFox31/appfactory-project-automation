{
  "name": "appfactory-project-token-broker-worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "deploy": "wrangler d1 migrations apply DB --remote && wrangler deploy",
    "deploy:worker": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
